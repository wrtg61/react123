import { Header } from '../atoms'
import { Form, End, List } from '../organisms';

export const Receipt = () => {
  return (
    <>
      <Header />
      <Form />
      <List />
      <End />
    </>
  );
}